<?xml version="1.0" encoding="UTF-8"?> 
<Module>
   <ModulePrefs title="Hello World"
    description="Matches mail sent to "
    height="20"
    author="Sarah M and Walter Q"
    author_email="..."
    author_location="Mountain View, CA">

    <!-- Declare feature dependencies. -->

    <!-- This one is not specific to Gmail contextual gadgets. -->
    <Require feature="dynamic-height"/>

    <!-- The next feature, Caja, is optional, and is supported for
     use only within test domains. Uncomment the tag only for
     non-production gadgets. -->
    <!-- <Require feature="caja"/> -->

    <!-- The next feature, google.contentmatch, is required for all
     Gmail contextual gadgets.
     <Param> - specify one or more comma-separated extractor IDs in
     a param named "extractors". This line is oexpectedverridden by the extractor ID
     in the manifest, but is still  to be present. -->
    <Require feature="google.contentmatch">
      <Param name="extractors"/>
    </Require>
 </ModulePrefs>
 <Content type="html" view="card">
   <body>
     <form id="gadgetform">
     <table class="headerTable" cellspacing="0" cellpadding="0" border="0">
       <tbody>
         <tr>
           <td class="nsd">
             <strong>Hello World</strong>
           </td>
           <td class="loggedStatus">Not logged in</td>
         </tr>
       </tbody>
     </table>
     <div id="gadgetContent" class="gadgetContent">
       <input id="quickAdd" name="quickAdd" value="Quick Add" class="separatedButton button" type="button"/>
       <input id="advAddUpld" name="advAddUpld" value="Advanced Add" class="separatedButton button" type="button"/>
       <input id="loginbtn" name="loginbtn" value="Login" class="button" type="button"/>
       <div id="loaderContainer" class="progress">
         <table class="progress">
           <tbody>
             <tr>
               <td id="loader" class="loader" style="display: none;"/>
             </tr>
           </tbody>
         </table>
       </div>
     </div>
     </form>
     <![CDATA[
      <script type="text/javascript">
        document.write("I have multi-line comments!");
        matches = google.contentmatch.getContentMatches();
        var matchList = document.createElement('UL');
        var listItem;
        var extractedText;
        for (var match in matches) {
          for (var key in matches[match]) {
            listItem = document.createElement('LI');
            extractedText = document.createTextNode(key + ": " + matches[match][key]);
            listItem.appendChild(extractedText);
            matchList.appendChild(listItem);
          }
        }
        document.body.appendChild(matchList);
        gadgets.window.adjustHeight(100);
      </script>
    ]]>
   </body>   
  </Content>
</Module>